"""
Phase 4: general-purpose secure web fetching for the URL Analyzer / URL
Safety Module — distinct from app/media/ (which is specifically the
Meta-media-CDN download path). See app/web/fetcher.py's module docstring for
why this is a separate module rather than an extension of app/media/.
"""
